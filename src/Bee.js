class Bee extends Grub {
  constructor() {
    // reference superclass
    super();
    // reassign age, color properties
    this.age = 5;
    this.color = 'yellow';
    // new job property
    this.job = 'Keep on growing';
  }
}
